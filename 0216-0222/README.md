## 本周完成了(工作的文件在0216-0222文件夹下面)
1. 训练了个resnet50(没有做特殊处理，裸resnet50)，利用了上周看的那篇论文里面训练集，验证集，测试集(测试集的三个(原版 再数字化 网络传输后)的准确率都在75%左右,验证集84%)
2. 然后搞了个stable-diffusion_v1-4来进行图片的生成,将错误识别的(AI的被识别成了real的那种),将其单独放入一个文件夹,作为一个新的数据集来训练(目前的baseline_imgs/train/ai的是我在我的本地电脑上生成的图片,质量不佳,大部分是一眼假,我打算之后在服务器上精炼下,至少眼睛过得去)

    原本是打算在服务器进行训练,但是现在(即2/22的晚十点左右,刚刚将模型加weights传到服务器,服务器连不了外网,我想着,以后得研究下,有没有更好的办法,先下载到本地再上传总是慢慢的,我好像之前有了解到了个代理的方法)
3. 注:由于github的单个文件大小有限制,所以一些文件不能上传到github
## 下周预计
1. 论文继续,读两篇(ICCV找一个,CVPR找一个(大方向是数据集怎么做))
2. baseline的第3次实验能做完(也就是融合这3篇论文思路后的简化版)